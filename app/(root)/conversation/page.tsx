import ConversataionFallback from "@/components/shared/conversation/ConverstaionFallback";
import React from "react";

type Props = {};

const ConversationsPage = (props: Props) => {
  return <ConversataionFallback />;
};

export default ConversationsPage;
